1 策略介绍： 在一系列股票里挑，那只符合金叉买入，死叉卖出，其他时间不动，最多买10支，每只1/10

import math
import random
import pandas as pd
from pandas import Series,DataFrame
import scipy.stats as stats

define initalize(context):
#初始化沪深300
 g.stocks_ttl = get_index_stocks('00300.XSHE')
 set_universe(g.stocks_ttl)
 set_commission(PerTrade(by_cost=0.0005, sell_cost=0.0013, min_cost=5))
 
 
